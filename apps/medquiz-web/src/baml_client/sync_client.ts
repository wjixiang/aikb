/*************************************************************************************************

Welcome to Baml! To use this generated code, please run one of the following:

$ npm install @boundaryml/baml
$ yarn add @boundaryml/baml
$ pnpm add @boundaryml/baml

*************************************************************************************************/

// This file was generated by BAML: please do not edit it. Instead, edit the
// BAML files and re-generate this code using: baml-cli generate
// You can install baml-cli with:
//  $ npm install @boundaryml/baml
//
/* eslint-disable */
// tslint:disable
// @ts-nocheck
// biome-ignore format: autogenerated code

import type {
  BamlRuntime,
  FunctionResult,
  BamlCtxManager,
  Image,
  Audio,
  Pdf,
  Video,
  ClientRegistry,
  Collector,
  FunctionLog,
} from '@boundaryml/baml';
import {
  toBamlError,
  BamlAbortError,
  type HTTPRequest,
} from '@boundaryml/baml';
import type {
  Checked,
  Check,
  RecursivePartialNull as MovedRecursivePartialNull,
} from './types';
import type * as types from './types';
import type {
  AgentDecision,
  AgentState,
  CriteriaScore,
  Decision,
  DocumentEvaluation,
  DocumentToRerank,
  Entity,
  EssayGrade,
  EssaySubmission,
  Explanation,
  GradingCriteria,
  GrammarCorrection,
  HyDE_query,
  HyDE_rewrite_query,
  KnowledgePoints,
  Knowledge_extraction_tool,
  Message,
  MutatedQuiz,
  Option,
  Quiz_retrieval_tool,
  Rawquiz_A,
  Related_documents,
  RelationshipSummary,
  RetrievedDocument,
  StepResponse,
  StructureAnalysis,
  Task,
  Textbook_semantic_search_tool,
  ToolCall,
  ToolDefinition,
  ToolList,
  ToolParameter,
  ToolResult,
  TranslationCorrection,
  VocabularySuggestion,
} from './types';
import type TypeBuilder from './type_builder';
import { HttpRequest, HttpStreamRequest } from './sync_request';
import { LlmResponseParser, LlmStreamParser } from './parser';
import {
  DO_NOT_USE_DIRECTLY_UNLESS_YOU_KNOW_WHAT_YOURE_DOING_CTX,
  DO_NOT_USE_DIRECTLY_UNLESS_YOU_KNOW_WHAT_YOURE_DOING_RUNTIME,
} from './globals';
import type * as events from './events';

/**
 * @deprecated Use RecursivePartialNull from 'baml_client/types' instead.
 * Example:
 * ```ts
 * import { RecursivePartialNull } from './baml_client/types'
 * ```
 */
export type RecursivePartialNull<T> = MovedRecursivePartialNull<T>;

type TickReason = 'Unknown';

type BamlCallOptions<WatchersT = never> = {
  tb?: TypeBuilder;
  clientRegistry?: ClientRegistry;
  collector?: Collector | Collector[];
  env?: Record<string, string | undefined>;
  tags?: Record<string, string>;
  signal?: AbortSignal;
  onTick?: (reason: TickReason, log: FunctionLog | null) => void;
  watchers?: WatchersT;
};

export class BamlSyncClient {
  private httpRequest: HttpRequest;
  private httpStreamRequest: HttpStreamRequest;
  private llmResponseParser: LlmResponseParser;
  private llmStreamParser: LlmStreamParser;
  private bamlOptions: BamlCallOptions;

  constructor(
    private runtime: BamlRuntime,
    private ctxManager: BamlCtxManager,
    bamlOptions?: BamlCallOptions,
  ) {
    this.httpRequest = new HttpRequest(runtime, ctxManager);
    this.httpStreamRequest = new HttpStreamRequest(runtime, ctxManager);
    this.llmResponseParser = new LlmResponseParser(runtime, ctxManager);
    this.llmStreamParser = new LlmStreamParser(runtime, ctxManager);
    this.bamlOptions = bamlOptions || {};
  }

  withOptions(bamlOptions: BamlCallOptions) {
    return new BamlSyncClient(this.runtime, this.ctxManager, bamlOptions);
  }

  /*
   * @deprecated NOT IMPLEMENTED as streaming must by async. We
   * are not providing an async version as we want to reserve the
   * right to provide a sync version in the future.
   */
  get stream() {
    throw new Error(
      "stream is not available in BamlSyncClient. Use `import { b } from 'baml_client/async_client",
    );
  }

  get request() {
    return this.httpRequest;
  }

  get streamRequest() {
    return this.httpStreamRequest;
  }

  get parse() {
    return this.llmResponseParser;
  }

  get parseStream() {
    return this.llmStreamParser;
  }

  AgentDecideNextAction(
    user_query: string,
    conversation_history: types.Message[],
    available_tools: types.ToolDefinition[],
    current_context: string,
    __baml_options__?: BamlCallOptions<never>,
  ): types.AgentDecision {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'AgentDecideNextAction',
        {
          user_query: user_query,
          conversation_history: conversation_history,
          available_tools: available_tools,
          current_context: current_context,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.AgentDecision;
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  AnnotateClass(
    quiz_content: string,
    __baml_options__?: BamlCallOptions<never>,
  ): '生理学' | '病理学' | '生物化学' | '内科学' | '外科学' {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'AnnotateClass',
        {
          quiz_content: quiz_content,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as
        | '生理学'
        | '病理学'
        | '生物化学'
        | '内科学'
        | '外科学';
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  CorrectTranslation(
    original: string,
    translation: string,
    context: string,
    __baml_options__?: BamlCallOptions<never>,
  ): types.TranslationCorrection {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'CorrectTranslation',
        {
          original: original,
          translation: translation,
          context: context,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.TranslationCorrection;
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  EvaluateDocumentSufficiency(
    quiz_content: string,
    documents: types.RetrievedDocument[],
    __baml_options__?: BamlCallOptions<never>,
  ): types.DocumentEvaluation {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'EvaluateDocumentSufficiency',
        {
          quiz_content: quiz_content,
          documents: documents,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.DocumentEvaluation;
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  ExecuteToolCall(
    tool_name: string,
    parameters: Record<string, types.ToolParameter>,
    tool_definitions: types.ToolDefinition[],
    __baml_options__?: BamlCallOptions<never>,
  ): types.ToolResult {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'ExecuteToolCall',
        {
          tool_name: tool_name,
          parameters: parameters,
          tool_definitions: tool_definitions,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.ToolResult;
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  ExtractMainEntity(
    chunk_text: string,
    entity_type: string[],
    __baml_options__?: BamlCallOptions<never>,
  ): types.Entity[] {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'ExtractMainEntity',
        {
          chunk_text: chunk_text,
          entity_type: entity_type,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.Entity[];
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  Extract_related_documents(
    quiz_content: string,
    documents: string[],
    __baml_options__?: BamlCallOptions<never>,
  ): types.Related_documents {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'Extract_related_documents',
        {
          quiz_content: quiz_content,
          documents: documents,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.Related_documents;
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  FilterRelatedChunks(
    quiz_content: string,
    documents: types.RetrievedDocument[],
    __baml_options__?: BamlCallOptions<never>,
  ): number[] {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'FilterRelatedChunks',
        {
          quiz_content: quiz_content,
          documents: documents,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as number[];
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  FinalizeAgentResponse(
    conversation_history: types.Message[],
    accumulated_information: string,
    __baml_options__?: BamlCallOptions<never>,
  ): types.StepResponse {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'FinalizeAgentResponse',
        {
          conversation_history: conversation_history,
          accumulated_information: accumulated_information,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.StepResponse;
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  GenerateAgentResponse(
    user_query: string,
    tool_results: types.ToolResult[],
    conversation_history: types.Message[],
    __baml_options__?: BamlCallOptions<never>,
  ): types.StepResponse {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'GenerateAgentResponse',
        {
          user_query: user_query,
          tool_results: tool_results,
          conversation_history: conversation_history,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.StepResponse;
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  GenerateAnswer(
    history?: string[] | null,
    query: string,
    documents: types.RetrievedDocument[],
    language: string,
    __baml_options__?: BamlCallOptions<never>,
  ): string {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'GenerateAnswer',
        {
          history: history ?? null,
          query: query,
          documents: documents,
          language: language,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as string;
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  GenerateAntiMemorizationQuiz(
    origin_quiz: string,
    related_chunks: string[],
    similar_quizes: string[],
    __baml_options__?: BamlCallOptions<never>,
  ): types.MutatedQuiz {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'GenerateAntiMemorizationQuiz',
        {
          origin_quiz: origin_quiz,
          related_chunks: related_chunks,
          similar_quizes: similar_quizes,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.MutatedQuiz;
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  GenerateExplanation(
    question: string,
    answer: string,
    options: string[],
    __baml_options__?: BamlCallOptions<never>,
  ): types.Explanation {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'GenerateExplanation',
        {
          question: question,
          answer: answer,
          options: options,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.Explanation;
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  GenerateExplanationForAnalysis(
    quiz: string,
    __baml_options__?: BamlCallOptions<never>,
  ): types.Explanation {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'GenerateExplanationForAnalysis',
        {
          quiz: quiz,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.Explanation;
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  GenerateQuizQuery(
    questionType: string,
    question: string,
    options: string[],
    answer: string,
    explanation: string,
    __baml_options__?: BamlCallOptions<never>,
  ): types.HyDE_query[] {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'GenerateQuizQuery',
        {
          questionType: questionType,
          question: question,
          options: options,
          answer: answer,
          explanation: explanation,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.HyDE_query[];
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  GenerateQuizVarient(
    origin_quiz: string,
    related_chunks: string[],
    similar_quizes: string[],
    __baml_options__?: BamlCallOptions<never>,
  ): types.MutatedQuiz {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'GenerateQuizVarient',
        {
          origin_quiz: origin_quiz,
          related_chunks: related_chunks,
          similar_quizes: similar_quizes,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.MutatedQuiz;
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  GenerateQuizzes_single(
    content: string,
    quiz_num: number,
    __baml_options__?: BamlCallOptions<never>,
  ): types.Rawquiz_A[] {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'GenerateQuizzes_single',
        {
          content: content,
          quiz_num: quiz_num,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.Rawquiz_A[];
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  GetGradingCriteriaSet(
    criteria_type: string,
    __baml_options__?: BamlCallOptions<never>,
  ): types.GradingCriteria[] {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'GetGradingCriteriaSet',
        {
          criteria_type: criteria_type,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.GradingCriteria[];
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  GradeEssayImage(
    essay: types.EssaySubmission,
    criteria: types.GradingCriteria[],
    __baml_options__?: BamlCallOptions<never>,
  ): types.EssayGrade {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'GradeEssayImage',
        {
          essay: essay,
          criteria: criteria,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.EssayGrade;
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  GradeEssayText(
    essay: types.EssaySubmission,
    criteria: types.GradingCriteria[],
    __baml_options__?: BamlCallOptions<never>,
  ): types.EssayGrade {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'GradeEssayText',
        {
          essay: essay,
          criteria: criteria,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.EssayGrade;
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  HyDE_rewrite(
    query: string,
    language: string,
    __baml_options__?: BamlCallOptions<never>,
  ): types.HyDE_rewrite_query {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'HyDE_rewrite',
        {
          query: query,
          language: language,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.HyDE_rewrite_query;
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  HyDE_rewriteForAnalysis(
    query: string,
    language: string,
    __baml_options__?: BamlCallOptions<never>,
  ): types.HyDE_rewrite_query {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'HyDE_rewriteForAnalysis',
        {
          query: query,
          language: language,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.HyDE_rewrite_query;
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  PlanNextStep(
    user_query: string,
    tasks_avaliable: types.Task[],
    __baml_options__?: BamlCallOptions<never>,
  ): types.Decision {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'PlanNextStep',
        {
          user_query: user_query,
          tasks_avaliable: tasks_avaliable,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.Decision;
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  RerankDocuments(
    query: string,
    documents: types.DocumentToRerank[],
    __baml_options__?: BamlCallOptions<never>,
  ): number[] {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'RerankDocuments',
        {
          query: query,
          documents: documents,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as number[];
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  SummarizeKnowledgePoint(
    quiz_content: string,
    quiz_explanation: string,
    __baml_options__?: BamlCallOptions<never>,
  ): types.KnowledgePoints[] {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'SummarizeKnowledgePoint',
        {
          quiz_content: quiz_content,
          quiz_explanation: quiz_explanation,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.KnowledgePoints[];
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  SummarizeKnowledgePointForAnalysis(
    quiz_content: string,
    quiz_explanation: string,
    __baml_options__?: BamlCallOptions<never>,
  ): types.KnowledgePoints[] {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'SummarizeKnowledgePointForAnalysis',
        {
          quiz_content: quiz_content,
          quiz_explanation: quiz_explanation,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.KnowledgePoints[];
    } catch (error: any) {
      throw toBamlError(error);
    }
  }

  SummarizeRelationships(
    quiz: string,
    knowledgePoints: string[],
    chunks: string[],
    __baml_options__?: BamlCallOptions<never>,
  ): types.RelationshipSummary {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided and reject for sync operations
      if (options.onTick) {
        throw new Error(
          'onTick is not supported for synchronous functions. Please use the async client instead.',
        );
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.callFunctionSync(
        'SummarizeRelationships',
        {
          quiz: quiz,
          knowledgePoints: knowledgePoints,
          chunks: chunks,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.RelationshipSummary;
    } catch (error: any) {
      throw toBamlError(error);
    }
  }
}

export const b = new BamlSyncClient(
  DO_NOT_USE_DIRECTLY_UNLESS_YOU_KNOW_WHAT_YOURE_DOING_RUNTIME,
  DO_NOT_USE_DIRECTLY_UNLESS_YOU_KNOW_WHAT_YOURE_DOING_CTX,
);
