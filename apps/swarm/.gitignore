# Swarm 应用 - Fastify + AgentRuntime

已完成的实现：

## 架构

```
apps/swarm/
├── src/
│   ├── server.ts              # 主入口 - Fastify 服务器启动
│   ├── config.ts              # 配置加载 - 从环境变量加载
│   ├── plugins/
│   │   └── agent-runtime.ts   # AgentRuntime Fastify 插件
│   └── routes/
│       ├── runtime.ts         # Runtime 管理 API
│       ├── agent.ts           # Agent 操作 API
│       ├── a2a.ts             # A2A 通信 API
│       └── health.ts          # 健康检查 API
├── test-api.sh                # API 测试脚本
├── README.md                  # 完整文档
└── package.json               # 依赖配置
```

## 核心设计

### 1. AgentRuntime 插件

将 AgentRuntime 集成到 Fastify 作为插件：

```typescript
// Fastify 实例获得以下装饰
fastify.agentRuntime   // AgentRuntime 实例
fastify.serverId       // 服务器唯一 ID
fastify.serverPort     // 服务器端口
```

### 2. HTTP API 映射

| HTTP 方法 | 端点 | RuntimeControlClient 方法 |
|-----------|------|--------------------------|
| GET | /api/runtime/stats | getStats() |
| GET | /api/runtime/agents | listAgents() |
| POST | /api/runtime/agents | createAgent() |
| DELETE | /api/runtime/agents/:id | destroyAgent() |
| POST | /api/a2a/task | sendA2ATask() |
| POST | /api/a2a/query | sendA2AQuery() |
| POST | /api/a2a/event | sendA2AEvent() |

### 3. 环境配置

```bash
# 单机模式（内存 MessageBus）
PORT=9400 MAX_AGENTS=50 OPENAI_API_KEY=xxx pnpm dev

# 分布式模式（Redis MessageBus）
A2A_MESSAGE_BUS_MODE=redis
A2A_REDIS_URL=redis://localhost:6379
PORT=9400 pnpm dev
```

## 使用示例

```bash
# 1. 启动服务器
cd apps/swarm
pnpm install
pnpm dev

# 2. 创建 Agent
curl -X POST http://localhost:9400/api/runtime/agents \
  -H "Content-Type: application/json" \
  -d '{
    "agent": {
      "name": "PubMed Agent",
      "type": "epidemiology",
      "sop": "Search PubMed for medical literature..."
    }
  }'

# 3. 发送 A2A 任务
curl -X POST http://localhost:9400/api/a2a/task \
  -H "Content-Type: application/json" \
  -d '{
    "targetAgentId": "agent-xxx",
    "taskId": "task-001",
    "description": "搜索文献",
    "input": {"query": "cancer", "limit": 10}
  }'

# 4. 运行 API 测试
chmod +x test-api.sh
./test-api.sh
```

## 快速开始

```bash
# 安装依赖
pnpm install

# 开发模式（自动重载）
pnpm dev

# 生产构建
pnpm build
pnpm start
```

完整的 API 文档请查看 `apps/swarm/README.md`。
