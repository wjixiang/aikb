"use client";

import QuizApp from "@/components/ai-coach/QuizApp";

export default function QuizSelectorTestPage() {
  // const [quiz, setQiuz] = useState<quiz[]>([])

  return (
    <div className="w-full h-screen -mb-10 overflow-y-auto">
      <QuizApp />
    </div>
  );
}
