/*************************************************************************************************

Welcome to Baml! To use this generated code, please run one of the following:

$ npm install @boundaryml/baml
$ yarn add @boundaryml/baml
$ pnpm add @boundaryml/baml

*************************************************************************************************/

// This file was generated by BAML: please do not edit it. Instead, edit the
// BAML files and re-generate this code using: baml-cli generate
// You can install baml-cli with:
//  $ npm install @boundaryml/baml
//
/* eslint-disable */
// tslint:disable
// @ts-nocheck
// biome-ignore format: autogenerated code

import type {
  BamlRuntime,
  FunctionResult,
  BamlCtxManager,
  ClientRegistry,
  Image,
  Audio,
  Pdf,
  Video,
  FunctionLog,
  HTTPRequest,
} from '@boundaryml/baml';
import {
  toBamlError,
  BamlStream,
  BamlAbortError,
  Collector,
} from '@boundaryml/baml';
import type {
  Checked,
  Check,
  RecursivePartialNull as MovedRecursivePartialNull,
} from './types';
import type { partial_types } from './partial_types';
import type * as types from './types';
import type {
  AgentDecision,
  AgentState,
  CriteriaScore,
  Decision,
  DocumentEvaluation,
  DocumentToRerank,
  Entity,
  EssayGrade,
  EssaySubmission,
  Explanation,
  GradingCriteria,
  GrammarCorrection,
  HyDE_query,
  HyDE_rewrite_query,
  KnowledgePoints,
  Knowledge_extraction_tool,
  Message,
  MutatedQuiz,
  Option,
  Quiz_retrieval_tool,
  Rawquiz_A,
  Related_documents,
  RelationshipSummary,
  RetrievedDocument,
  StepResponse,
  StructureAnalysis,
  Task,
  Textbook_semantic_search_tool,
  ToolCall,
  ToolDefinition,
  ToolList,
  ToolParameter,
  ToolResult,
  TranslationCorrection,
  VocabularySuggestion,
} from './types';
import type TypeBuilder from './type_builder';
import { AsyncHttpRequest, AsyncHttpStreamRequest } from './async_request';
import { LlmResponseParser, LlmStreamParser } from './parser';
import {
  DO_NOT_USE_DIRECTLY_UNLESS_YOU_KNOW_WHAT_YOURE_DOING_CTX,
  DO_NOT_USE_DIRECTLY_UNLESS_YOU_KNOW_WHAT_YOURE_DOING_RUNTIME,
} from './globals';

/**
 * @deprecated Use RecursivePartialNull from 'baml_client/types' instead.
 */
export type RecursivePartialNull<T> = MovedRecursivePartialNull<T>;

type TickReason = 'Unknown';

type BamlCallOptions = {
  tb?: TypeBuilder;
  clientRegistry?: ClientRegistry;
  collector?: Collector | Collector[];
  env?: Record<string, string | undefined>;
  tags?: Record<string, string>;
  signal?: AbortSignal;
  onTick?: (reason: TickReason, log: FunctionLog | null) => void;
};

export class BamlAsyncClient {
  private runtime: BamlRuntime;
  private ctxManager: BamlCtxManager;
  private streamClient: BamlStreamClient;
  private httpRequest: AsyncHttpRequest;
  private httpStreamRequest: AsyncHttpStreamRequest;
  private llmResponseParser: LlmResponseParser;
  private llmStreamParser: LlmStreamParser;
  private bamlOptions: BamlCallOptions;

  constructor(
    runtime: BamlRuntime,
    ctxManager: BamlCtxManager,
    bamlOptions?: BamlCallOptions,
  ) {
    this.runtime = runtime;
    this.ctxManager = ctxManager;
    this.streamClient = new BamlStreamClient(runtime, ctxManager, bamlOptions);
    this.httpRequest = new AsyncHttpRequest(runtime, ctxManager);
    this.httpStreamRequest = new AsyncHttpStreamRequest(runtime, ctxManager);
    this.llmResponseParser = new LlmResponseParser(runtime, ctxManager);
    this.llmStreamParser = new LlmStreamParser(runtime, ctxManager);
    this.bamlOptions = bamlOptions || {};
  }

  withOptions(bamlOptions: BamlCallOptions) {
    return new BamlAsyncClient(this.runtime, this.ctxManager, bamlOptions);
  }

  get stream() {
    return this.streamClient;
  }

  get request() {
    return this.httpRequest;
  }

  get streamRequest() {
    return this.httpStreamRequest;
  }

  get parse() {
    return this.llmResponseParser;
  }

  get parseStream() {
    return this.llmStreamParser;
  }

  async AgentDecideNextAction(
    user_query: string,
    conversation_history: types.Message[],
    available_tools: types.ToolDefinition[],
    current_context: string,
    __baml_options__?: BamlCallOptions,
  ): Promise<types.AgentDecision> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.AgentDecideNextAction(
          user_query,
          conversation_history,
          available_tools,
          current_context,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'AgentDecideNextAction',
        {
          user_query: user_query,
          conversation_history: conversation_history,
          available_tools: available_tools,
          current_context: current_context,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.AgentDecision;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async AnnotateClass(
    quiz_content: string,
    __baml_options__?: BamlCallOptions,
  ): Promise<'生理学' | '病理学' | '生物化学' | '内科学' | '外科学'> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.AnnotateClass(
          quiz_content,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'AnnotateClass',
        {
          quiz_content: quiz_content,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as
        | '生理学'
        | '病理学'
        | '生物化学'
        | '内科学'
        | '外科学';
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async CorrectTranslation(
    original: string,
    translation: string,
    context: string,
    __baml_options__?: BamlCallOptions,
  ): Promise<types.TranslationCorrection> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.CorrectTranslation(
          original,
          translation,
          context,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'CorrectTranslation',
        {
          original: original,
          translation: translation,
          context: context,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.TranslationCorrection;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async EvaluateDocumentSufficiency(
    quiz_content: string,
    documents: types.RetrievedDocument[],
    __baml_options__?: BamlCallOptions,
  ): Promise<types.DocumentEvaluation> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.EvaluateDocumentSufficiency(
          quiz_content,
          documents,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'EvaluateDocumentSufficiency',
        {
          quiz_content: quiz_content,
          documents: documents,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.DocumentEvaluation;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async ExecuteToolCall(
    tool_name: string,
    parameters: Record<string, types.ToolParameter>,
    tool_definitions: types.ToolDefinition[],
    __baml_options__?: BamlCallOptions,
  ): Promise<types.ToolResult> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.ExecuteToolCall(
          tool_name,
          parameters,
          tool_definitions,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'ExecuteToolCall',
        {
          tool_name: tool_name,
          parameters: parameters,
          tool_definitions: tool_definitions,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.ToolResult;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async ExtractMainEntity(
    chunk_text: string,
    entity_type: string[],
    __baml_options__?: BamlCallOptions,
  ): Promise<types.Entity[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.ExtractMainEntity(
          chunk_text,
          entity_type,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'ExtractMainEntity',
        {
          chunk_text: chunk_text,
          entity_type: entity_type,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.Entity[];
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async Extract_related_documents(
    quiz_content: string,
    documents: string[],
    __baml_options__?: BamlCallOptions,
  ): Promise<types.Related_documents> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.Extract_related_documents(
          quiz_content,
          documents,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'Extract_related_documents',
        {
          quiz_content: quiz_content,
          documents: documents,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.Related_documents;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async FilterRelatedChunks(
    quiz_content: string,
    documents: types.RetrievedDocument[],
    __baml_options__?: BamlCallOptions,
  ): Promise<number[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.FilterRelatedChunks(
          quiz_content,
          documents,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'FilterRelatedChunks',
        {
          quiz_content: quiz_content,
          documents: documents,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as number[];
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async FinalizeAgentResponse(
    conversation_history: types.Message[],
    accumulated_information: string,
    __baml_options__?: BamlCallOptions,
  ): Promise<types.StepResponse> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.FinalizeAgentResponse(
          conversation_history,
          accumulated_information,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'FinalizeAgentResponse',
        {
          conversation_history: conversation_history,
          accumulated_information: accumulated_information,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.StepResponse;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async GenerateAgentResponse(
    user_query: string,
    tool_results: types.ToolResult[],
    conversation_history: types.Message[],
    __baml_options__?: BamlCallOptions,
  ): Promise<types.StepResponse> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.GenerateAgentResponse(
          user_query,
          tool_results,
          conversation_history,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'GenerateAgentResponse',
        {
          user_query: user_query,
          tool_results: tool_results,
          conversation_history: conversation_history,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.StepResponse;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async GenerateAnswer(
    history?: string[] | null,
    query: string,
    documents: types.RetrievedDocument[],
    language: string,
    __baml_options__?: BamlCallOptions,
  ): Promise<string> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.GenerateAnswer(
          history,
          query,
          documents,
          language,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'GenerateAnswer',
        {
          history: history ?? null,
          query: query,
          documents: documents,
          language: language,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as string;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async GenerateAntiMemorizationQuiz(
    origin_quiz: string,
    related_chunks: string[],
    similar_quizes: string[],
    __baml_options__?: BamlCallOptions,
  ): Promise<types.MutatedQuiz> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.GenerateAntiMemorizationQuiz(
          origin_quiz,
          related_chunks,
          similar_quizes,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'GenerateAntiMemorizationQuiz',
        {
          origin_quiz: origin_quiz,
          related_chunks: related_chunks,
          similar_quizes: similar_quizes,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.MutatedQuiz;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async GenerateExplanation(
    question: string,
    answer: string,
    options: string[],
    __baml_options__?: BamlCallOptions,
  ): Promise<types.Explanation> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.GenerateExplanation(
          question,
          answer,
          options,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'GenerateExplanation',
        {
          question: question,
          answer: answer,
          options: options,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.Explanation;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async GenerateExplanationForAnalysis(
    quiz: string,
    __baml_options__?: BamlCallOptions,
  ): Promise<types.Explanation> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.GenerateExplanationForAnalysis(
          quiz,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'GenerateExplanationForAnalysis',
        {
          quiz: quiz,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.Explanation;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async GenerateQuizQuery(
    questionType: string,
    question: string,
    options: string[],
    answer: string,
    explanation: string,
    __baml_options__?: BamlCallOptions,
  ): Promise<types.HyDE_query[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.GenerateQuizQuery(
          questionType,
          question,
          options,
          answer,
          explanation,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'GenerateQuizQuery',
        {
          questionType: questionType,
          question: question,
          options: options,
          answer: answer,
          explanation: explanation,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.HyDE_query[];
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async GenerateQuizVarient(
    origin_quiz: string,
    related_chunks: string[],
    similar_quizes: string[],
    __baml_options__?: BamlCallOptions,
  ): Promise<types.MutatedQuiz> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.GenerateQuizVarient(
          origin_quiz,
          related_chunks,
          similar_quizes,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'GenerateQuizVarient',
        {
          origin_quiz: origin_quiz,
          related_chunks: related_chunks,
          similar_quizes: similar_quizes,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.MutatedQuiz;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async GenerateQuizzes_single(
    content: string,
    quiz_num: number,
    __baml_options__?: BamlCallOptions,
  ): Promise<types.Rawquiz_A[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.GenerateQuizzes_single(
          content,
          quiz_num,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'GenerateQuizzes_single',
        {
          content: content,
          quiz_num: quiz_num,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.Rawquiz_A[];
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async GetGradingCriteriaSet(
    criteria_type: string,
    __baml_options__?: BamlCallOptions,
  ): Promise<types.GradingCriteria[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.GetGradingCriteriaSet(
          criteria_type,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'GetGradingCriteriaSet',
        {
          criteria_type: criteria_type,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.GradingCriteria[];
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async GradeEssayImage(
    essay: types.EssaySubmission,
    criteria: types.GradingCriteria[],
    __baml_options__?: BamlCallOptions,
  ): Promise<types.EssayGrade> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.GradeEssayImage(
          essay,
          criteria,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'GradeEssayImage',
        {
          essay: essay,
          criteria: criteria,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.EssayGrade;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async GradeEssayText(
    essay: types.EssaySubmission,
    criteria: types.GradingCriteria[],
    __baml_options__?: BamlCallOptions,
  ): Promise<types.EssayGrade> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.GradeEssayText(
          essay,
          criteria,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'GradeEssayText',
        {
          essay: essay,
          criteria: criteria,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.EssayGrade;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async HyDE_rewrite(
    query: string,
    language: string,
    __baml_options__?: BamlCallOptions,
  ): Promise<types.HyDE_rewrite_query> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.HyDE_rewrite(
          query,
          language,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'HyDE_rewrite',
        {
          query: query,
          language: language,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.HyDE_rewrite_query;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async HyDE_rewriteForAnalysis(
    query: string,
    language: string,
    __baml_options__?: BamlCallOptions,
  ): Promise<types.HyDE_rewrite_query> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.HyDE_rewriteForAnalysis(
          query,
          language,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'HyDE_rewriteForAnalysis',
        {
          query: query,
          language: language,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.HyDE_rewrite_query;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async PlanNextStep(
    user_query: string,
    tasks_avaliable: types.Task[],
    __baml_options__?: BamlCallOptions,
  ): Promise<types.Decision> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.PlanNextStep(
          user_query,
          tasks_avaliable,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'PlanNextStep',
        {
          user_query: user_query,
          tasks_avaliable: tasks_avaliable,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.Decision;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async RerankDocuments(
    query: string,
    documents: types.DocumentToRerank[],
    __baml_options__?: BamlCallOptions,
  ): Promise<number[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.RerankDocuments(
          query,
          documents,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'RerankDocuments',
        {
          query: query,
          documents: documents,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as number[];
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async SummarizeKnowledgePoint(
    quiz_content: string,
    quiz_explanation: string,
    __baml_options__?: BamlCallOptions,
  ): Promise<types.KnowledgePoints[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.SummarizeKnowledgePoint(
          quiz_content,
          quiz_explanation,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'SummarizeKnowledgePoint',
        {
          quiz_content: quiz_content,
          quiz_explanation: quiz_explanation,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.KnowledgePoints[];
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async SummarizeKnowledgePointForAnalysis(
    quiz_content: string,
    quiz_explanation: string,
    __baml_options__?: BamlCallOptions,
  ): Promise<types.KnowledgePoints[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.SummarizeKnowledgePointForAnalysis(
          quiz_content,
          quiz_explanation,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'SummarizeKnowledgePointForAnalysis',
        {
          quiz_content: quiz_content,
          quiz_explanation: quiz_explanation,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.KnowledgePoints[];
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async SummarizeRelationships(
    quiz: string,
    knowledgePoints: string[],
    chunks: string[],
    __baml_options__?: BamlCallOptions,
  ): Promise<types.RelationshipSummary> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.SummarizeRelationships(
          quiz,
          knowledgePoints,
          chunks,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'SummarizeRelationships',
        {
          quiz: quiz,
          knowledgePoints: knowledgePoints,
          chunks: chunks,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
      );
      return raw.parsed(false) as types.RelationshipSummary;
    } catch (error) {
      throw toBamlError(error);
    }
  }
}

class BamlStreamClient {
  private runtime: BamlRuntime;
  private ctxManager: BamlCtxManager;
  private bamlOptions: BamlCallOptions;

  constructor(
    runtime: BamlRuntime,
    ctxManager: BamlCtxManager,
    bamlOptions?: BamlCallOptions,
  ) {
    this.runtime = runtime;
    this.ctxManager = ctxManager;
    this.bamlOptions = bamlOptions || {};
  }

  AgentDecideNextAction(
    user_query: string,
    conversation_history: types.Message[],
    available_tools: types.ToolDefinition[],
    current_context: string,
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.AgentDecision, types.AgentDecision> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for AgentDecideNextAction',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'AgentDecideNextAction',
        {
          user_query: user_query,
          conversation_history: conversation_history,
          available_tools: available_tools,
          current_context: current_context,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.AgentDecision, types.AgentDecision>(
        raw,
        (a): partial_types.AgentDecision => a,
        (a): types.AgentDecision => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  AnnotateClass(
    quiz_content: string,
    __baml_options__?: BamlCallOptions,
  ): BamlStream<
    '生理学' | '病理学' | '生物化学' | '内科学' | '外科学',
    '生理学' | '病理学' | '生物化学' | '内科学' | '外科学'
  > {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for AnnotateClass',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'AnnotateClass',
        {
          quiz_content: quiz_content,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<
        '生理学' | '病理学' | '生物化学' | '内科学' | '外科学',
        '生理学' | '病理学' | '生物化学' | '内科学' | '外科学'
      >(
        raw,
        (a): '生理学' | '病理学' | '生物化学' | '内科学' | '外科学' => a,
        (a): '生理学' | '病理学' | '生物化学' | '内科学' | '外科学' => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  CorrectTranslation(
    original: string,
    translation: string,
    context: string,
    __baml_options__?: BamlCallOptions,
  ): BamlStream<
    partial_types.TranslationCorrection,
    types.TranslationCorrection
  > {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for CorrectTranslation',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'CorrectTranslation',
        {
          original: original,
          translation: translation,
          context: context,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<
        partial_types.TranslationCorrection,
        types.TranslationCorrection
      >(
        raw,
        (a): partial_types.TranslationCorrection => a,
        (a): types.TranslationCorrection => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  EvaluateDocumentSufficiency(
    quiz_content: string,
    documents: types.RetrievedDocument[],
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.DocumentEvaluation, types.DocumentEvaluation> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for EvaluateDocumentSufficiency',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'EvaluateDocumentSufficiency',
        {
          quiz_content: quiz_content,
          documents: documents,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<
        partial_types.DocumentEvaluation,
        types.DocumentEvaluation
      >(
        raw,
        (a): partial_types.DocumentEvaluation => a,
        (a): types.DocumentEvaluation => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  ExecuteToolCall(
    tool_name: string,
    parameters: Record<string, types.ToolParameter>,
    tool_definitions: types.ToolDefinition[],
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.ToolResult, types.ToolResult> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for ExecuteToolCall',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'ExecuteToolCall',
        {
          tool_name: tool_name,
          parameters: parameters,
          tool_definitions: tool_definitions,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.ToolResult, types.ToolResult>(
        raw,
        (a): partial_types.ToolResult => a,
        (a): types.ToolResult => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  ExtractMainEntity(
    chunk_text: string,
    entity_type: string[],
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.Entity[], types.Entity[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for ExtractMainEntity',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'ExtractMainEntity',
        {
          chunk_text: chunk_text,
          entity_type: entity_type,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.Entity[], types.Entity[]>(
        raw,
        (a): partial_types.Entity[] => a,
        (a): types.Entity[] => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  Extract_related_documents(
    quiz_content: string,
    documents: string[],
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.Related_documents, types.Related_documents> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for Extract_related_documents',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'Extract_related_documents',
        {
          quiz_content: quiz_content,
          documents: documents,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<
        partial_types.Related_documents,
        types.Related_documents
      >(
        raw,
        (a): partial_types.Related_documents => a,
        (a): types.Related_documents => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  FilterRelatedChunks(
    quiz_content: string,
    documents: types.RetrievedDocument[],
    __baml_options__?: BamlCallOptions,
  ): BamlStream<number[], number[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for FilterRelatedChunks',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'FilterRelatedChunks',
        {
          quiz_content: quiz_content,
          documents: documents,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<number[], number[]>(
        raw,
        (a): number[] => a,
        (a): number[] => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  FinalizeAgentResponse(
    conversation_history: types.Message[],
    accumulated_information: string,
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.StepResponse, types.StepResponse> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for FinalizeAgentResponse',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'FinalizeAgentResponse',
        {
          conversation_history: conversation_history,
          accumulated_information: accumulated_information,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.StepResponse, types.StepResponse>(
        raw,
        (a): partial_types.StepResponse => a,
        (a): types.StepResponse => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  GenerateAgentResponse(
    user_query: string,
    tool_results: types.ToolResult[],
    conversation_history: types.Message[],
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.StepResponse, types.StepResponse> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for GenerateAgentResponse',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'GenerateAgentResponse',
        {
          user_query: user_query,
          tool_results: tool_results,
          conversation_history: conversation_history,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.StepResponse, types.StepResponse>(
        raw,
        (a): partial_types.StepResponse => a,
        (a): types.StepResponse => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  GenerateAnswer(
    history?: string[] | null,
    query: string,
    documents: types.RetrievedDocument[],
    language: string,
    __baml_options__?: BamlCallOptions,
  ): BamlStream<string, string> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for GenerateAnswer',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'GenerateAnswer',
        {
          history: history ?? null,
          query: query,
          documents: documents,
          language: language,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<string, string>(
        raw,
        (a): string => a,
        (a): string => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  GenerateAntiMemorizationQuiz(
    origin_quiz: string,
    related_chunks: string[],
    similar_quizes: string[],
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.MutatedQuiz, types.MutatedQuiz> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for GenerateAntiMemorizationQuiz',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'GenerateAntiMemorizationQuiz',
        {
          origin_quiz: origin_quiz,
          related_chunks: related_chunks,
          similar_quizes: similar_quizes,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.MutatedQuiz, types.MutatedQuiz>(
        raw,
        (a): partial_types.MutatedQuiz => a,
        (a): types.MutatedQuiz => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  GenerateExplanation(
    question: string,
    answer: string,
    options: string[],
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.Explanation, types.Explanation> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for GenerateExplanation',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'GenerateExplanation',
        {
          question: question,
          answer: answer,
          options: options,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.Explanation, types.Explanation>(
        raw,
        (a): partial_types.Explanation => a,
        (a): types.Explanation => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  GenerateExplanationForAnalysis(
    quiz: string,
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.Explanation, types.Explanation> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for GenerateExplanationForAnalysis',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'GenerateExplanationForAnalysis',
        {
          quiz: quiz,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.Explanation, types.Explanation>(
        raw,
        (a): partial_types.Explanation => a,
        (a): types.Explanation => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  GenerateQuizQuery(
    questionType: string,
    question: string,
    options: string[],
    answer: string,
    explanation: string,
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.HyDE_query[], types.HyDE_query[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for GenerateQuizQuery',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'GenerateQuizQuery',
        {
          questionType: questionType,
          question: question,
          options: options,
          answer: answer,
          explanation: explanation,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.HyDE_query[], types.HyDE_query[]>(
        raw,
        (a): partial_types.HyDE_query[] => a,
        (a): types.HyDE_query[] => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  GenerateQuizVarient(
    origin_quiz: string,
    related_chunks: string[],
    similar_quizes: string[],
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.MutatedQuiz, types.MutatedQuiz> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for GenerateQuizVarient',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'GenerateQuizVarient',
        {
          origin_quiz: origin_quiz,
          related_chunks: related_chunks,
          similar_quizes: similar_quizes,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.MutatedQuiz, types.MutatedQuiz>(
        raw,
        (a): partial_types.MutatedQuiz => a,
        (a): types.MutatedQuiz => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  GenerateQuizzes_single(
    content: string,
    quiz_num: number,
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.Rawquiz_A[], types.Rawquiz_A[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for GenerateQuizzes_single',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'GenerateQuizzes_single',
        {
          content: content,
          quiz_num: quiz_num,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.Rawquiz_A[], types.Rawquiz_A[]>(
        raw,
        (a): partial_types.Rawquiz_A[] => a,
        (a): types.Rawquiz_A[] => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  GetGradingCriteriaSet(
    criteria_type: string,
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.GradingCriteria[], types.GradingCriteria[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for GetGradingCriteriaSet',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'GetGradingCriteriaSet',
        {
          criteria_type: criteria_type,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<
        partial_types.GradingCriteria[],
        types.GradingCriteria[]
      >(
        raw,
        (a): partial_types.GradingCriteria[] => a,
        (a): types.GradingCriteria[] => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  GradeEssayImage(
    essay: types.EssaySubmission,
    criteria: types.GradingCriteria[],
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.EssayGrade, types.EssayGrade> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for GradeEssayImage',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'GradeEssayImage',
        {
          essay: essay,
          criteria: criteria,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.EssayGrade, types.EssayGrade>(
        raw,
        (a): partial_types.EssayGrade => a,
        (a): types.EssayGrade => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  GradeEssayText(
    essay: types.EssaySubmission,
    criteria: types.GradingCriteria[],
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.EssayGrade, types.EssayGrade> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for GradeEssayText',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'GradeEssayText',
        {
          essay: essay,
          criteria: criteria,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.EssayGrade, types.EssayGrade>(
        raw,
        (a): partial_types.EssayGrade => a,
        (a): types.EssayGrade => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  HyDE_rewrite(
    query: string,
    language: string,
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.HyDE_rewrite_query, types.HyDE_rewrite_query> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error('Error in onTick callback for HyDE_rewrite', error);
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'HyDE_rewrite',
        {
          query: query,
          language: language,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<
        partial_types.HyDE_rewrite_query,
        types.HyDE_rewrite_query
      >(
        raw,
        (a): partial_types.HyDE_rewrite_query => a,
        (a): types.HyDE_rewrite_query => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  HyDE_rewriteForAnalysis(
    query: string,
    language: string,
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.HyDE_rewrite_query, types.HyDE_rewrite_query> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for HyDE_rewriteForAnalysis',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'HyDE_rewriteForAnalysis',
        {
          query: query,
          language: language,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<
        partial_types.HyDE_rewrite_query,
        types.HyDE_rewrite_query
      >(
        raw,
        (a): partial_types.HyDE_rewrite_query => a,
        (a): types.HyDE_rewrite_query => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  PlanNextStep(
    user_query: string,
    tasks_avaliable: types.Task[],
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.Decision, types.Decision> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error('Error in onTick callback for PlanNextStep', error);
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'PlanNextStep',
        {
          user_query: user_query,
          tasks_avaliable: tasks_avaliable,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.Decision, types.Decision>(
        raw,
        (a): partial_types.Decision => a,
        (a): types.Decision => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  RerankDocuments(
    query: string,
    documents: types.DocumentToRerank[],
    __baml_options__?: BamlCallOptions,
  ): BamlStream<number[], number[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for RerankDocuments',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'RerankDocuments',
        {
          query: query,
          documents: documents,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<number[], number[]>(
        raw,
        (a): number[] => a,
        (a): number[] => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  SummarizeKnowledgePoint(
    quiz_content: string,
    quiz_explanation: string,
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.KnowledgePoints[], types.KnowledgePoints[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for SummarizeKnowledgePoint',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'SummarizeKnowledgePoint',
        {
          quiz_content: quiz_content,
          quiz_explanation: quiz_explanation,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<
        partial_types.KnowledgePoints[],
        types.KnowledgePoints[]
      >(
        raw,
        (a): partial_types.KnowledgePoints[] => a,
        (a): types.KnowledgePoints[] => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  SummarizeKnowledgePointForAnalysis(
    quiz_content: string,
    quiz_explanation: string,
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.KnowledgePoints[], types.KnowledgePoints[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for SummarizeKnowledgePointForAnalysis',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'SummarizeKnowledgePointForAnalysis',
        {
          quiz_content: quiz_content,
          quiz_explanation: quiz_explanation,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<
        partial_types.KnowledgePoints[],
        types.KnowledgePoints[]
      >(
        raw,
        (a): partial_types.KnowledgePoints[] => a,
        (a): types.KnowledgePoints[] => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  SummarizeRelationships(
    quiz: string,
    knowledgePoints: string[],
    chunks: string[],
    __baml_options__?: BamlCallOptions,
  ): BamlStream<partial_types.RelationshipSummary, types.RelationshipSummary> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for SummarizeRelationships',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'SummarizeRelationships',
        {
          quiz: quiz,
          knowledgePoints: knowledgePoints,
          chunks: chunks,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<
        partial_types.RelationshipSummary,
        types.RelationshipSummary
      >(
        raw,
        (a): partial_types.RelationshipSummary => a,
        (a): types.RelationshipSummary => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }
}

export const b = new BamlAsyncClient(
  DO_NOT_USE_DIRECTLY_UNLESS_YOU_KNOW_WHAT_YOURE_DOING_RUNTIME,
  DO_NOT_USE_DIRECTLY_UNLESS_YOU_KNOW_WHAT_YOURE_DOING_CTX,
);
