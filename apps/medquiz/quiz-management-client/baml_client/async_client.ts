/*************************************************************************************************

Welcome to Baml! To use this generated code, please run one of the following:

$ npm install @boundaryml/baml
$ yarn add @boundaryml/baml
$ pnpm add @boundaryml/baml

*************************************************************************************************/

// This file was generated by BAML: please do not edit it. Instead, edit the
// BAML files and re-generate this code using: baml-cli generate
// You can install baml-cli with:
//  $ npm install @boundaryml/baml
//
/* eslint-disable */
// tslint:disable
// @ts-nocheck
// biome-ignore format: autogenerated code

import type {
  BamlRuntime,
  FunctionResult,
  BamlCtxManager,
  ClientRegistry,
  Image,
  Audio,
  Pdf,
  Video,
  FunctionLog,
  HTTPRequest,
} from '@boundaryml/baml';
import {
  toBamlError,
  BamlStream,
  BamlAbortError,
  Collector,
} from '@boundaryml/baml';
import type {
  Checked,
  Check,
  RecursivePartialNull as MovedRecursivePartialNull,
} from './types';
import type { partial_types } from './partial_types';
import type * as types from './types';
import type {
  A3PreQuiz,
  BPreQuiz,
  BasicQuiz,
  Chunks,
  ContentSlice,
  QAunit,
  QAunitForB,
  QuestionAnswerPair,
  QuestionAnswerSlice,
  QuestionAnswerWithExplanationPair,
  QuestionAnswerWithExplanationSlice,
  QuizAnalysis,
  QuizOptions,
  Resume,
  SplitText,
} from './types';
import type TypeBuilder from './type_builder';
import { AsyncHttpRequest, AsyncHttpStreamRequest } from './async_request';
import { LlmResponseParser, LlmStreamParser } from './parser';
import {
  DO_NOT_USE_DIRECTLY_UNLESS_YOU_KNOW_WHAT_YOURE_DOING_CTX,
  DO_NOT_USE_DIRECTLY_UNLESS_YOU_KNOW_WHAT_YOURE_DOING_RUNTIME,
} from './globals';
import type * as events from './events';

/**
 * @deprecated Use RecursivePartialNull from 'baml_client/types' instead.
 */
export type RecursivePartialNull<T> = MovedRecursivePartialNull<T>;

type TickReason = 'Unknown';

type BamlCallOptions<WatchersT = never> = {
  tb?: TypeBuilder;
  clientRegistry?: ClientRegistry;
  collector?: Collector | Collector[];
  env?: Record<string, string | undefined>;
  tags?: Record<string, string>;
  signal?: AbortSignal;
  onTick?: (reason: TickReason, log: FunctionLog | null) => void;
  watchers?: WatchersT;
};

export class BamlAsyncClient {
  private runtime: BamlRuntime;
  private ctxManager: BamlCtxManager;
  private streamClient: BamlStreamClient;
  private httpRequest: AsyncHttpRequest;
  private httpStreamRequest: AsyncHttpStreamRequest;
  private llmResponseParser: LlmResponseParser;
  private llmStreamParser: LlmStreamParser;
  private bamlOptions: BamlCallOptions;

  constructor(
    runtime: BamlRuntime,
    ctxManager: BamlCtxManager,
    bamlOptions?: BamlCallOptions,
  ) {
    this.runtime = runtime;
    this.ctxManager = ctxManager;
    this.streamClient = new BamlStreamClient(runtime, ctxManager, bamlOptions);
    this.httpRequest = new AsyncHttpRequest(runtime, ctxManager);
    this.httpStreamRequest = new AsyncHttpStreamRequest(runtime, ctxManager);
    this.llmResponseParser = new LlmResponseParser(runtime, ctxManager);
    this.llmStreamParser = new LlmStreamParser(runtime, ctxManager);
    this.bamlOptions = bamlOptions || {};
  }

  withOptions(bamlOptions: BamlCallOptions) {
    return new BamlAsyncClient(this.runtime, this.ctxManager, bamlOptions);
  }

  get stream() {
    return this.streamClient;
  }

  get request() {
    return this.httpRequest;
  }

  get streamRequest() {
    return this.httpStreamRequest;
  }

  get parse() {
    return this.llmResponseParser;
  }

  get parseStream() {
    return this.llmStreamParser;
  }

  async ConvertToA3Quiz(
    question: string,
    answer: string,
    explanation?: string | null,
    __baml_options__?: BamlCallOptions<never>,
  ): Promise<types.A3PreQuiz> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.ConvertToA3Quiz(
          question,
          answer,
          explanation,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'ConvertToA3Quiz',
        {
          question: question,
          answer: answer,
          explanation: explanation ?? null,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.A3PreQuiz;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async ConvertToBQuiz(
    question: string,
    answer: string,
    explanation?: string | null,
    __baml_options__?: BamlCallOptions<never>,
  ): Promise<types.BPreQuiz> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.ConvertToBQuiz(
          question,
          answer,
          explanation,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'ConvertToBQuiz',
        {
          question: question,
          answer: answer,
          explanation: explanation ?? null,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.BPreQuiz;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async ConvertToBasicQuiz(
    question: string,
    answer: string,
    explanation?: string | null,
    __baml_options__?: BamlCallOptions<never>,
  ): Promise<types.BasicQuiz> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.ConvertToBasicQuiz(
          question,
          answer,
          explanation,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'ConvertToBasicQuiz',
        {
          question: question,
          answer: answer,
          explanation: explanation ?? null,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.BasicQuiz;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async ExtractResume(
    resume: string,
    __baml_options__?: BamlCallOptions<never>,
  ): Promise<types.Resume> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.ExtractResume(resume, __baml_options__);

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'ExtractResume',
        {
          resume: resume,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.Resume;
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async MatchQuestionsAnswers(
    input: types.SplitText,
    __baml_options__?: BamlCallOptions<never>,
  ): Promise<types.QuestionAnswerSlice[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.MatchQuestionsAnswers(
          input,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'MatchQuestionsAnswers',
        {
          input: input,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.QuestionAnswerSlice[];
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async MatchQuestionsAnswersWithExplanation(
    input: types.SplitText,
    __baml_options__?: BamlCallOptions<never>,
  ): Promise<types.QuestionAnswerWithExplanationSlice[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.MatchQuestionsAnswersWithExplanation(
          input,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'MatchQuestionsAnswersWithExplanation',
        {
          input: input,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.QuestionAnswerWithExplanationSlice[];
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async SplitPage(
    parsedQuestionText: string,
    parsedAnswerText: string,
    chunkNum: number,
    __baml_options__?: BamlCallOptions<never>,
  ): Promise<types.Chunks[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.SplitPage(
          parsedQuestionText,
          parsedAnswerText,
          chunkNum,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'SplitPage',
        {
          parsedQuestionText: parsedQuestionText,
          parsedAnswerText: parsedAnswerText,
          chunkNum: chunkNum,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as types.Chunks[];
    } catch (error) {
      throw toBamlError(error);
    }
  }

  async SplitQuestions(
    questions_text: string,
    __baml_options__?: BamlCallOptions<never>,
  ): Promise<string[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      // Check if onTick is provided - route through streaming if so
      if (options.onTick) {
        const stream = this.stream.SplitQuestions(
          questions_text,
          __baml_options__,
        );

        return await stream.getFinalResponse();
      }

      const collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];
      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = await this.runtime.callFunction(
        'SplitQuestions',
        {
          questions_text: questions_text,
        },
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        options.watchers,
      );
      return raw.parsed(false) as string[];
    } catch (error) {
      throw toBamlError(error);
    }
  }
}

class BamlStreamClient {
  private runtime: BamlRuntime;
  private ctxManager: BamlCtxManager;
  private bamlOptions: BamlCallOptions;

  constructor(
    runtime: BamlRuntime,
    ctxManager: BamlCtxManager,
    bamlOptions?: BamlCallOptions,
  ) {
    this.runtime = runtime;
    this.ctxManager = ctxManager;
    this.bamlOptions = bamlOptions || {};
  }

  ConvertToA3Quiz(
    question: string,
    answer: string,
    explanation?: string | null,
    __baml_options__?: BamlCallOptions<never>,
  ): BamlStream<partial_types.A3PreQuiz, types.A3PreQuiz> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for ConvertToA3Quiz',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'ConvertToA3Quiz',
        {
          question: question,
          answer: answer,
          explanation: explanation ?? null,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.A3PreQuiz, types.A3PreQuiz>(
        raw,
        (a): partial_types.A3PreQuiz => a,
        (a): types.A3PreQuiz => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  ConvertToBQuiz(
    question: string,
    answer: string,
    explanation?: string | null,
    __baml_options__?: BamlCallOptions<never>,
  ): BamlStream<partial_types.BPreQuiz, types.BPreQuiz> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for ConvertToBQuiz',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'ConvertToBQuiz',
        {
          question: question,
          answer: answer,
          explanation: explanation ?? null,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.BPreQuiz, types.BPreQuiz>(
        raw,
        (a): partial_types.BPreQuiz => a,
        (a): types.BPreQuiz => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  ConvertToBasicQuiz(
    question: string,
    answer: string,
    explanation?: string | null,
    __baml_options__?: BamlCallOptions<never>,
  ): BamlStream<partial_types.BasicQuiz, types.BasicQuiz> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for ConvertToBasicQuiz',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'ConvertToBasicQuiz',
        {
          question: question,
          answer: answer,
          explanation: explanation ?? null,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.BasicQuiz, types.BasicQuiz>(
        raw,
        (a): partial_types.BasicQuiz => a,
        (a): types.BasicQuiz => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  ExtractResume(
    resume: string,
    __baml_options__?: BamlCallOptions<never>,
  ): BamlStream<partial_types.Resume, types.Resume> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for ExtractResume',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'ExtractResume',
        {
          resume: resume,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.Resume, types.Resume>(
        raw,
        (a): partial_types.Resume => a,
        (a): types.Resume => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  MatchQuestionsAnswers(
    input: types.SplitText,
    __baml_options__?: BamlCallOptions<never>,
  ): BamlStream<
    partial_types.QuestionAnswerSlice[],
    types.QuestionAnswerSlice[]
  > {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for MatchQuestionsAnswers',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'MatchQuestionsAnswers',
        {
          input: input,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<
        partial_types.QuestionAnswerSlice[],
        types.QuestionAnswerSlice[]
      >(
        raw,
        (a): partial_types.QuestionAnswerSlice[] => a,
        (a): types.QuestionAnswerSlice[] => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  MatchQuestionsAnswersWithExplanation(
    input: types.SplitText,
    __baml_options__?: BamlCallOptions<never>,
  ): BamlStream<
    partial_types.QuestionAnswerWithExplanationSlice[],
    types.QuestionAnswerWithExplanationSlice[]
  > {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for MatchQuestionsAnswersWithExplanation',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'MatchQuestionsAnswersWithExplanation',
        {
          input: input,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<
        partial_types.QuestionAnswerWithExplanationSlice[],
        types.QuestionAnswerWithExplanationSlice[]
      >(
        raw,
        (a): partial_types.QuestionAnswerWithExplanationSlice[] => a,
        (a): types.QuestionAnswerWithExplanationSlice[] => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  SplitPage(
    parsedQuestionText: string,
    parsedAnswerText: string,
    chunkNum: number,
    __baml_options__?: BamlCallOptions<never>,
  ): BamlStream<partial_types.Chunks[], types.Chunks[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error('Error in onTick callback for SplitPage', error);
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'SplitPage',
        {
          parsedQuestionText: parsedQuestionText,
          parsedAnswerText: parsedAnswerText,
          chunkNum: chunkNum,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<partial_types.Chunks[], types.Chunks[]>(
        raw,
        (a): partial_types.Chunks[] => a,
        (a): types.Chunks[] => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }

  SplitQuestions(
    questions_text: string,
    __baml_options__?: BamlCallOptions<never>,
  ): BamlStream<string[], string[]> {
    try {
      const options = { ...this.bamlOptions, ...(__baml_options__ || {}) };
      const signal = options.signal;

      if (signal?.aborted) {
        throw new BamlAbortError('Operation was aborted', signal.reason);
      }

      let collector = options.collector
        ? Array.isArray(options.collector)
          ? options.collector
          : [options.collector]
        : [];

      let onTickWrapper: (() => void) | undefined;

      // Create collector and wrap onTick if provided
      if (options.onTick) {
        const tickCollector = new Collector('on-tick-collector');
        collector = [...collector, tickCollector];

        onTickWrapper = () => {
          const log = tickCollector.last;
          if (log) {
            try {
              options.onTick!('Unknown', log);
            } catch (error) {
              console.error(
                'Error in onTick callback for SplitQuestions',
                error,
              );
            }
          }
        };
      }

      const rawEnv = __baml_options__?.env
        ? { ...process.env, ...__baml_options__.env }
        : { ...process.env };
      const env: Record<string, string> = Object.fromEntries(
        Object.entries(rawEnv).filter(([_, value]) => value !== undefined) as [
          string,
          string,
        ][],
      );
      const raw = this.runtime.streamFunction(
        'SplitQuestions',
        {
          questions_text: questions_text,
        },
        undefined,
        this.ctxManager.cloneContext(),
        options.tb?.__tb(),
        options.clientRegistry,
        collector,
        options.tags || {},
        env,
        signal,
        onTickWrapper,
      );
      return new BamlStream<string[], string[]>(
        raw,
        (a): string[] => a,
        (a): string[] => a,
        this.ctxManager.cloneContext(),
        options.signal,
      );
    } catch (error) {
      throw toBamlError(error);
    }
  }
}

export const b = new BamlAsyncClient(
  DO_NOT_USE_DIRECTLY_UNLESS_YOU_KNOW_WHAT_YOURE_DOING_RUNTIME,
  DO_NOT_USE_DIRECTLY_UNLESS_YOU_KNOW_WHAT_YOURE_DOING_CTX,
);
