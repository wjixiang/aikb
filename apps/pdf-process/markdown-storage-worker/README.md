# markdown-storage-worker

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build markdown-storage-worker` to build the library.

## Running unit tests

Run `nx test markdown-storage-worker` to execute the unit tests via [Vitest](https://vitest.dev/).
