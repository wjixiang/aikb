# item-chunk-embed-service

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build item-chunk-embed-service` to build the library.

## Running unit tests

Run `nx test item-chunk-embed-service` to execute the unit tests via [Vitest](https://vitest.dev/).
