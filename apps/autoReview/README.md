# autoReview

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build autoReview` to build the library.

## Running unit tests

Run `nx test autoReview` to execute the unit tests via [Vitest](https://vitest.dev/).
