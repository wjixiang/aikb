# embedding

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build embedding` to build the library.

## Running unit tests

Run `nx test embedding` to execute the unit tests via [Vitest](https://vitest.dev/).
