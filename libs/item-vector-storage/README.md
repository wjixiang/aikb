# item-vector-storage

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build item-vector-storage` to build the library.

## Running unit tests

Run `nx test item-vector-storage` to execute the unit tests via [Vitest](https://vitest.dev/).
