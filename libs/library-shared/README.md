# library-shared

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build library-shared` to build the library.

## Running unit tests

Run `nx test library-shared` to execute the unit tests via [Vitest](https://vitest.dev/).
