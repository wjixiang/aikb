import { BookViewerComponent } from "../bookshelfComponents";

describe(BookViewerComponent, () => {
    it("should catch sid-effects errors and render it into context", async () => {
        const component = new BookViewerComponent()
    })
})