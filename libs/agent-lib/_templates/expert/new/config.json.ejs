---
to: src/expert/builtin/<%= name %>/config.json
---
{
  "id": "<%= name %>",
  "displayName": "<%= name.replace(/-([a-z])/g, (g) => ' ' + g[1].toUpperCase()).replace(/^./, (s) => s.toUpperCase()) %>",
  "description": "TODO: Add description",
  "category": "general",
  "tags": <%- JSON.stringify(name.split('-')) %>,
  "triggers": [],
  "whenToUse": "TODO: Add when to use this expert",
  "components": []
}
