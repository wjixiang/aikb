# chunking

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build chunking` to build the library.

## Running unit tests

Run `nx test chunking` to execute the unit tests via [Vitest](https://vitest.dev/).
